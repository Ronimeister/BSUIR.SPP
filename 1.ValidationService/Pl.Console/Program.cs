using System;
using System.Collections.Generic;
using System.ComponentModel;
using System.ComponentModel.DataAnnotations;
using System.Linq;
using System.Text;
using System.Threading.Tasks;
using ValidationCOre;
using ValidationCOre.Service;

namespace Pl.Console
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
            Person person = new Person("1234", "Alexey", 19);
            person.PassportId = null;
            System.Console.WriteLine(person.ToString());

            var results = new List<ValidationResult>();
            if (!ValidationService.TryValidateObject(person, new ValidationContext(person), results, true))
            {
                foreach (var error in results)
                {
                    System.Console.WriteLine(error.ErrorMessage);
                }
            }
            System.Console.Read();
            */
        }
    }
}